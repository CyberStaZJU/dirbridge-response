# DirBridge 下一步计划

## 首要目标：解决新数据集实验

### 1. 明确实验契约

- 确认新数据集名称、原始格式、类别数、输入形状、客户端数量、最小客户端样本数和训练/测试划分。
- 确认采用 Dir-Skew 还是 FedScale 延迟轨迹，以及并发数、缓冲区大小、轮数和随机种子。
- 在 `configs/` 增加一份可审计 YAML，避免把参数散落在命令行或脚本中。

### 2. 先打通数据和模型

- 在 `builders/dataset_builder.py` 增加或修正数据集加载与客户端索引。
- 在 `builders/model_builder.py` 增加匹配模型，并先做一个 batch 的形状与标签范围检查。
- 若是自然数据集，明确特征缓存位置；原始数据和缓存放在仓库外。

### 3. 进行分层验证

1. 数据读取 smoke：客户端数、样本数、类别分布、训练/测试可读。
2. 单客户端训练：确认 loss 下降或至少完成反向传播。
3. 聚合 smoke：用少量客户端和短轮次运行 FedBuff 与 DirBridge。
4. 延迟与方向偏斜检查：确认日志中 `random_cost`、方向偏斜指标和模拟时间有效。
5. 单种子完整短跑通过后，再扩展到五种子正式实验。

### 4. 记录与判定

- 每次运行记录配置、代码状态、数据版本、设备和输出位置。
- 用 `ds/summarize_tail10.py` 等工具生成汇总；显著性分析前先确认每个种子的结果完整。
- 将失败运行留在待诊断位置，修复后再转入正式结果；不以 smoke 结果替代论文结果。

## 后续目标

- 运行 `scripts/check_artifact.sh`，修复可复现性或隐私扫描问题。
- 完成新数据集后，再复核 CIFAR Dir-Skew 与 FEMNIST/GSpeech FedScale 主线是否仍可运行。
- 建立本地 Git 仓库或关联远端后，做提交级一致性检查并记录 commit。

