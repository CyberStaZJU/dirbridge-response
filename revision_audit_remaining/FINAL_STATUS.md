# Remaining hard-task final status

## E1

The FEMNIST validation development matrix is complete: 24/24 verified runs, with validation-selected development configurations recorded in `revision_audit_non_e2/e1_final/`. No final five-seed evaluation was launched in this task. E1 is therefore development-complete but not final-evaluation-complete.

## E4

The existing E4 results remain usable as a real-FedScale-profile-driven controlled data–system coupling experiment. The repository's prior E4 audit reports formula equivalence and corrected paired statistics; the remaining-scope canonical clean 70-row table and official physical-payload chain still require consolidation. No automatic 70-run training rerun was launched. GSpeech non-finite-loss causes remain unresolved because the raw logs lack logits, parameters, BN buffers, and checkpoints around the event.

## E5

The existing 65 runs remain usable for sensitivity analysis. B comparisons are fixed-server-round results and do not establish equal-work superiority. K0=7 is a pre-specified heuristic rather than an empirical optimum; K0=12/16 are higher in the stored setting. Sketch dimension is robust over the tested range. Finite-buffer sampling and the existing Phi decomposition prevent nonzero Phi from being treated as proof of persistent latency-induced skew.

## CA2FL

The existing five-step running-mean equivalence test passed for the incremental state path with repeated client identities and strict float64 tolerance. A separate canonical production-entry-path test is still needed before claiming full production-path equivalence.

## E6

The repository contains a direct CPU tensor ledger for representative method-owned state and the prior resource interpretation. It shows the expected CA2FL O(NP) cache state plus O(P) mean and O(BP) calibration. A production-initialized actual-state ledger and arithmetic consistency check remain to be consolidated in the remaining-scope directory; CUDA values are unavailable in the local resumed environment.

## E3

`ENDPOINTS NOT VALID — do not run five-level training` is recorded as a blocked-data verdict. The authorized desktop was inaccessible, compatible CIFAR-100 client/profile artifacts were unavailable locally, and PyTorch was absent locally. No endpoint replay, training, or five-level matrix was launched. This verdict must not be interpreted as a scientific equality result.

## Rerun boundary

No E2, E4 70-run, E5 65-run, E1 full fair-tuning, or E3 five-level training rerun was launched in the remaining-hard-task audit. The only completed new training work is the already-running/completed E1 FEMNIST validation development matrix, which is documented separately.
