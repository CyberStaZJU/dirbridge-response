# E3 Status

E3 is paused. The earlier quota/coupling proposal was rejected because it manually selected clients by direction group, which would not test a natural arrival process. No E3 result is included in the completed-results tables.

A future E3 must construct the label-cost coupling naturally, preserve ordinary client sampling, record the realized arrival mixture, and separate the experiment from the E1-E5 audited results.
