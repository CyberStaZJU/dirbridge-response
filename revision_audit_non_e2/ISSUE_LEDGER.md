# Non-E2 Revision Audit Issue Ledger

Scope: E1, E3, E4, E5, and E6 only. The E2 experiment and its result files are intentionally excluded from this audit.

| Area | Status | Evidence | Impact | Correction / current boundary | Rerun? |
|---|---|---|---|---|---|
| E1 tuning budget counted as unique configurations | CONFIRMED / partially corrected | `e1-fair-tuning/commands_dev_*.sh`; the recorded 60 CIFAR submissions are unique, while FEMNIST has 47 unique effective configurations among 60 submissions | The phrase “20 configurations per method per dataset” is not uniformly true for the FEMNIST command file | Report submitted runs and unique effective configurations separately; do not claim perfect symmetry without qualification | No for audit; rerun only if a complete symmetric search is required |
| E1 FedBuff had no server learning-rate control | SUPERSEDED | Current `algorithm/fedbuff.py` multiplies `aggregated_diff` by `global_lr`; current `algorithm/dirbridge.py` does likewise | Older explanations misdescribe current code and cannot support server-step conclusions | Use the current exposed `global_lr`; label older unit-step claims as historical | No |
| E1 CA2FL server step was tunable | ALREADY FIXED | Current `algorithm/ca2fl.py` maps `global_lr` to `eta`, defaulting to 1.0 | Historical CA2FL results from the hard-coded harness are not evidence of a tuned sweep | Preserve old results as historical; use repaired code for any new formal claim | Targeted smoke passed; formal rerun not automatically required |
| E1 FADAS mechanism fully established | UNRESOLVED / overstated | `algorithm/fadas.py` updates moments before applying `m/sqrt(vhat)+eps`; old prose calls the denominator approximately epsilon without reporting its measured distribution | The prior causal explanation is stronger than the available evidence | State that larger server steps were empirically unstable; do not claim the exact adaptive-state cause without denominator logs | Targeted diagnostic rerun if mechanism is needed |
| E1 formal tuning symmetry | CONFIRMED limitation | `e1-fair-tuning/DESIGN_AND_RESULTS.md` states DirBridge retained prior defaults while baselines were tuned | Advantage estimates are conditional and may favor DirBridge | Report matched configurations and the asymmetry explicitly | No for existing results |
| E1 test/validation separation | CONFIRMED limitation | E1 selection criterion is tail-10 **test accuracy** on a development seed; no separate validation split is documented | Development selection is test-guided, not independent validation | Call it development/test-guided selection; do not call it held-out validation | No |
| E3 completed controlled coupling experiment | CONFIRMED not completed | `e3-profile-coupling/STATUS.md` says E3 is paused after a quota-based proposal was rejected | No E3 causal or monotonic coupling claim is supported | Keep E3 out of completed-results tables; an endpoint diagnostic is a prerequisite | Yes, only if the reviewer requires E3 evidence |
| E4 scope as natural real latency evidence | CONFIRMED overstatement risk | `e4-profile-coupling/RESOURCE_AUDIT_AND_SCOPE.md` says profiles are capability samples and durations are derived | The 70 runs do not establish naturally observed label-latency coupling or measured end-to-end latency | Call E4 a real-FedScale-profile-driven controlled coupling experiment | No |
| E4 public reproduction path | ALREADY FIXED for parser/code path | Current `utils/options.py` exposes `--fedscale_profile_coupling`; current `utils/fedscale_trace.py` consumes it | The old provenance warning is stale for the current artifact, though run identity remains archived | Keep the executed-code identity and add a parser smoke to the verification record | No |
| E4 profile unit formula | CONFIRMED implementation/documentation boundary | Current code uses `3*batch*steps*computation/1000 + (upload+download)/communication`; resource audit records capability units and fixed payloads | Numeric duration depends on the chosen payload convention and is not an observed network latency | Document fixed payload and capability-derived duration; do not call it a trace | Targeted unit test recommended |
| E4 FEMNIST narrative | CONFIRMED overstatement | Per-seed tail-10 values in `e4-profile-coupling/DESIGN_AND_RESULTS.md` show DirBridge wins 2/5 against FedBuff | “Higher more often” is false for this pairing | Report paired mean, CI, and wins/losses | No |
| E4 GSpeech NaN loss | CONFIRMED limitation | E4 design notes transient `Test loss nan`; finite accuracy files alone do not prove numerical health | Accuracy summaries must not be called fully numerically healthy | Report finite accuracy with explicit loss-health limitation; root cause remains unresolved unless raw diagnostics are available | Targeted diagnostic rerun only if causal diagnosis is required |
| E5 fixed-threshold control provenance | ALREADY FIXED in current public path, historical identity still required | Current parser and `algorithm/dirbridge.py` consume `dirbridge_buffer_delay_limit`; E5 document identifies the pre-E2 code identity | The historical 65-run matrix must retain its original identity | Do not silently relabel old runs as current-tree runs | No |
| E5 K0=1 equivalence | UNRESOLVED | Existing prose attributes low K0=1 performance to centroid flips, but one group has no alternate group assignment | The mechanism claim is not established | Add deterministic K0=1 vs gated FedBuff equivalence test before retaining the explanation | Targeted test required |
| E5 Phi/coverage as persistent skew | UNRESOLVED / overstated | Existing values may be ordinary finite-buffer variation; no null calibration is stored in the current record | Small-buffer Phi cannot alone prove persistent latency-induced bias | Recompute finite-population null and persistent-vs-fluctuation decomposition if raw monitor data are available | Reanalysis, not training |
| E5 B comparison as equal work | CONFIRMED overstatement | E5 itself says B=20 consumes more client work and reverses ranking under matched simulated work | Fixed-round accuracy is not equal-work accuracy | Report server-round, returned-update, and simulated-time axes separately; retain only metrics actually available | Reanalysis |
| E5 d_s=2048 empirically optimal | REFUTED / overstated | E5 table gives d_s=8192 a higher mean than 2048 | “Flat over a broad range” is supported; “optimal” is not | Use stability/overhead language, not optimum language | No |
| E6 standalone matrix complete | CONFIRMED not completed | `e6-resource-accounting/STATUS.md` explicitly says no independent E6 matrix exists | E6 cannot be presented as a completed training result | Use the E4 resource audit as partial supporting evidence only | No |
| E6 CA2FL incremental implementation | ALREADY FIXED in current worktree | `algorithm/ca2fl.py` maintains `cache_mean`, uses O(BP) calibration, preserves repeated IDs, and updates mean after replacement | Complexity claim is now code-backed, but old timings are not timings for this implementation | Keep old resource timings attributed to the old implementation; use the new deterministic equivalence test | Targeted test passed remotely |
| E6 device peak terminology | CONFIRMED overstatement | `main_fed.py` currently records `torch.cuda.max_memory_allocated()` as `device_peak_memory_mb` | A historical peak is not current persistent allocation | Distinguish current allocated, reserved, and historical peak; avoid treating peak alone as persistent state | Targeted resource probe if needed |
